<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-sdk tools:overrideLibrary="ai.moonshine.voice" />

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <application
        android:name=".SmolChatApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:localeConfig="@xml/locales_config"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AMM"
        tools:targetApi="31">
        <activity
            android:name=".ui.screens.manage_asr.ManageASRActivity"
            android:exported="false"
            android:label="@string/title_activity_app_settings"
            android:theme="@style/Theme.AMM" />
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/Theme.AMM">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ui.screens.chat.ChatActivity"
            android:exported="true"
            android:label="💬 LLM">

            <!-- This intent-filter is used to receive text from other apps -->
            <!-- The received text is then sent to a new chat -->
            <intent-filter>
                <action android:name="android.intent.action.SEND" />

                <category android:name="android.intent.category.DEFAULT" />

                <data android:mimeType="text/plain" />
            </intent-filter>
        </activity>
        <activity android:name=".ui.screens.model_download.DownloadModelActivity" />
        <activity android:name=".ui.screens.manage_tasks.ManageTasksActivity" />
        <activity
            android:name=".ui.screens.vision_hub.VisionHubActivity"
            android:exported="false"
            android:label="🎯 Vision Hub"
            android:theme="@style/Theme.AMM" />
        <activity
            android:name=".ui.screens.browser.BrowserActivity"
            android:exported="false"
            android:label="🌐 Browser"
            android:theme="@style/Theme.AMM" />

        <service
            android:name=".llm.HttpService"
            android:enabled="true"
            android:exported="false"
            android:foregroundServiceType="dataSync" />

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>
    </application>

</manifest>